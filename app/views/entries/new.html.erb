<% title "New Entry" %>
<% subtitle "Fields marked with #{content_tag :em, 'asterisks (*)'} are required.".html_safe %>

<%= simple_form_for @entry, :validate => true do |f| %>
	<%= render 'form', f: f %>
<% end %>

