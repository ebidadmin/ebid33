<nav class="topbar" data-dropdown="dropdown" data-scrollspy="scrollspy">
  <div class="topbar-inner">
    <div class="container">
      <a class="brand" href="#">E-Bid</a>
      <ul class="nav">
				<%= content_tag :li, class: 'dropdown' do %>
					<%= link_to 'Entries', entries_path(s: 'Online'), class: 'dropdown-toggle' %>
					<ul class="dropdown-menu">
						<%= content_tag :li, link_to('Online', entries_path(s: 'Online')) %>
						<%= content_tag :li, link_to('All', entries_path) %>
					</ul>
				<% end %>
				<%= content_tag :li, link_to('Bids', bids_path) %>
				<%= content_tag :li, class: 'dropdown' do %>
					<%= link_to 'Orders', orders_path(s: 'New'), class: 'dropdown-toggle' %>
					<ul class="dropdown-menu">
						<%= content_tag :li, link_to('New', orders_path(s: 'Online')) %>
						<%= content_tag :li, link_to('For Delivery', orders_path(s: 'For-Delivery')) %>
					</ul>
				<% end %>
				<%= content_tag :li, class: 'dropdown' do %>
					<%= link_to 'Payments', orders_path(s: 'Delivered'), class: 'dropdown-toggle' %>
					<ul class="dropdown-menu">
						<%= content_tag :li, link_to('Within Term', orders_path(s: 'Online')) %>
						<%= content_tag :li, link_to('Overdue', orders_path(s: 'For-Delivery')) %>
						<%= content_tag :li, link_to('Paid', orders_path(s: 'Delivered')) %>
					</ul>
				<% end %>
				<%= content_tag :li, class: 'dropdown' do %>
					<%= link_to 'Fees', fees_path(s: 'PO'), class: 'dropdown-toggle' %>
					<ul class="dropdown-menu">
						<%= content_tag :li, link_to('PO Fees', fees_path(s: 'PO')) %>
						<%= content_tag :li, link_to('Canvassing Fees', fees_path(s: 'Declines')) %>
					</ul>
				<% end %>
       </ul>

			<ul class="nav secondary-nav">
				<% if user_signed_in? %>
					<%= content_tag :li, link_to("Logout #{current_user.username.upcase}", destroy_user_session_path, :method=>'delete') %>
				<% else %>
					<%= content_tag :li, link_to('Login', new_user_session_path) %>
				<% end %>

				<% if user_signed_in? %>
				  <%= content_tag :li, link_to('Change Password', edit_user_registration_path) %>
				  <%= content_tag :li, link_to('Edit My Account', edit_user_path(current_user)) %>
				<% else %>
				  <%= content_tag :li, link_to('Sign Up', new_user_registration_path) %>
				<% end %>
      </ul>
    </div>
  </div>
</nav>
